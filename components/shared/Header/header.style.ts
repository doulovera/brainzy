import styled from 'styled-components';

export const HeaderStyled = styled.header`
  position: fixed;
  top: 0;
  display: flex;
  align-items: center;
  height: var(--header-height);
  width: 100%;
  padding-left: 3rem;

  font-size: 2rem;
  font-weight: 800;
`;
